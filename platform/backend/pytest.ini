[pytest]
# Make sure `import app` works whether pytest is launched from the repo root or
# from the backend directory by adding both the backend dir and the app package
# itself to PYTHONPATH.
pythonpath = . app ..
